import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
   container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
   },
   wrapperContainer: {
      flex: 1,
      backgroundColor: '#fff',
      width: '98%',
   },
   scrollViewContent: {
      alignItems: 'center',
      justifyContent: 'center',
   },
   orderContainer: {
      flex: 1,
      width: '95%',
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: '#fff',
      padding: 0,
   }
});